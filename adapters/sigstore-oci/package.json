{
  "name": "@oci-distribution/sigstore-oci",
  "version": "0.1.0",
  "description": "A drop-in-compatible reimplementation of @sigstore/oci's public API, built on the oci-distribution client. Kept separate from the core library.",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bdehamer/oci-distribution-js.git",
    "directory": "adapters/sigstore-oci"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {
    "oci-distribution": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0"
  }
}
